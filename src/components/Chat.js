import React from 'react';
import styled from 'styled-components';

function Chat () {
  return (
    <ChatContainer>
      <h1>Chat Screens</h1>
    </ChatContainer>
  );
}

export default Chat;

const ChatContainer = styled.div``;
